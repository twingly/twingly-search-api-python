language: python
sudo: false
addons:
  apt_packages:
    - pandoc

python:
  - "2.7"
  - "3.5"

install:
  - pip install pypandoc
  - travis_retry pip install .
  - pip install -r test_requirements.txt

script:
  - make test
